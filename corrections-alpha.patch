diff --git a/app/(dashboard)/analytics/page.tsx b/app/(dashboard)/analytics/page.tsx
index f748035..558cdd3 100644
--- a/app/(dashboard)/analytics/page.tsx
+++ b/app/(dashboard)/analytics/page.tsx
@@ -113,9 +113,17 @@ export default function AnalyticsPage() {
       const sorted = Object.values(stats).sort((a, b) => b.revenue - a.revenue).slice(0, 5);
       setTopProducts(sorted);
     }).finally(() => setLoadingTop(false));
-  }, [tenantId, sales.length]);
+    // Fix : dépendre de sales.length seul pouvait manquer une mise à jour si le
+    // contenu change sans changer le nombre de ventes (ex. annulation + nouvelle
+    // vente dans la même période via le listener temps réel) — top produits
+    // restait alors obsolète.
+  }, [tenantId, sales]);
 
   const completedSales = useMemo(() => sales.filter(s => s.status === 'COMPLETED'), [sales]);
+  // `now` est volontairement recréé à chaque rendu (pas mémoïsé) pour garantir
+  // que le useMemo plus bas reste basé sur la date réelle du rendu — voir le
+  // fix sur la donnée obsolète documenté à l'intérieur de ce useMemo.
+  // eslint-disable-next-line react-hooks/exhaustive-deps
   const now = new Date();
   const monthsCount = period === '3m' ? 3 : period === '6m' ? 6 : 12;
 
@@ -132,7 +140,9 @@ export default function AnalyticsPage() {
       data.push({ month: MONTHS_FR[d.getMonth()], ca, marge: Math.round(ca - cout), ventes: monthSales.length });
     }
     return data;
-  }, [completedSales, period]);
+    // `now` est recréé à chaque rendu (pas figé) ; l'inclure ici est donc correct
+    // et sans risque de boucle, il ne fait que refléter la date courante du rendu.
+  }, [completedSales, monthsCount, now]);
 
   const weeklyData = useMemo(() => {
     const days = ['Lun','Mar','Mer','Jeu','Ven','Sam','Dim'];
diff --git a/app/(dashboard)/credits/page.tsx b/app/(dashboard)/credits/page.tsx
index fe93a04..a39fba1 100644
--- a/app/(dashboard)/credits/page.tsx
+++ b/app/(dashboard)/credits/page.tsx
@@ -137,6 +137,10 @@ export default function CreditsPage() {
     return onSnapshot(q, (snap) => {
       setVersements(snap.docs.map(d => ({ id: d.id, ...d.data() })) as Versement[]);
     });
+    // Seul selected?.id détermine la requête ; ajouter l'objet `selected` entier
+    // provoquerait un rechargement à chaque nouvelle référence (ex. re-fetch de
+    // la liste des crédits) même quand il s'agit du même crédit sélectionné.
+    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [tenantId, selected?.id]);
 
   // ─── Filtres ────────────────────────────────────────────────────────────────
diff --git a/app/(dashboard)/customers/[id]/page.tsx b/app/(dashboard)/customers/[id]/page.tsx
index b463fd8..2a2a471 100644
--- a/app/(dashboard)/customers/[id]/page.tsx
+++ b/app/(dashboard)/customers/[id]/page.tsx
@@ -73,7 +73,7 @@ export default function CustomerDetailPage() {
       where('customerId', '==', id),
       orderBy('createdAt', 'desc')
     )).then(snap => setQuotes(snap.docs.map(d => ({ id: d.id, ...d.data() })) as Quote[]));
-  }, [tenantId, id]);
+  }, [tenantId, id, router]);
 
   if (isLoading) return (
     <DashboardLayout>
diff --git a/app/(dashboard)/pos/page.tsx b/app/(dashboard)/pos/page.tsx
index c4a60cc..e188a2e 100644
--- a/app/(dashboard)/pos/page.tsx
+++ b/app/(dashboard)/pos/page.tsx
@@ -1,6 +1,6 @@
 'use client';
 
-import { useState, useEffect } from 'react';
+import { useState, useEffect, useRef } from 'react';
 import {
   Search, ShoppingCart, Trash2, Plus, Minus,
   User, X, CheckCircle2, RefreshCw, Package,
@@ -111,6 +111,12 @@ export default function POSPage() {
     // eslint-disable-next-line react-hooks/exhaustive-deps
   }, []);
 
+  // Fix : ce ref garde toujours la dernière version de handleCheckout,
+  // même si l'effet clavier plus bas ne se relance pas à chaque frappe —
+  // sans ça (closure figée), Ctrl+Entrée pouvait valider un paiement avec
+  // un mode/montant/client déjà changé mais pas encore pris en compte.
+  const handleCheckoutRef = useRef<() => void>(() => {});
+
   // Raccourcis clavier POS
   useEffect(() => {
     const handleKey = (e: KeyboardEvent) => {
@@ -128,7 +134,7 @@ export default function POSPage() {
       }
       // Ctrl+Entrée → valider le paiement si dialog ouvert
       if ((e.ctrlKey || e.metaKey) && e.key === 'Enter' && showPayment && !isProcessing) {
-        handleCheckout();
+        handleCheckoutRef.current();
       }
       // Ctrl+Suppr → vider le panier
       if ((e.ctrlKey || e.metaKey) && e.key === 'Delete' && !showPayment) {
@@ -137,7 +143,7 @@ export default function POSPage() {
     };
     window.addEventListener('keydown', handleKey);
     return () => window.removeEventListener('keydown', handleKey);
-  }, [showPayment, showCustomerPicker, showSuccess, isProcessing, items.length]);
+  }, [showPayment, showCustomerPicker, showSuccess, isProcessing, items.length, clearCart]);
 
   // ─── Listeners ──────────────────────────────────────────────────────────
   useEffect(() => {
@@ -321,6 +327,12 @@ export default function POSPage() {
     }
   };
 
+  // Garde toujours handleCheckoutRef à jour avec la dernière closure (voir
+  // le fix plus haut sur le raccourci clavier Ctrl+Entrée).
+  useEffect(() => {
+    handleCheckoutRef.current = handleCheckout;
+  });
+
   const displayCustomerName = (c: Customer) =>
     c.customerType === 'BUSINESS' ? c.companyName! : `${c.firstName || ''} ${c.lastName || ''}`.trim();
 
diff --git a/app/(dashboard)/products/new/page.tsx b/app/(dashboard)/products/new/page.tsx
index e6a9886..b61187f 100644
--- a/app/(dashboard)/products/new/page.tsx
+++ b/app/(dashboard)/products/new/page.tsx
@@ -9,6 +9,6 @@ export default function ProductNewPage() {
   const router = useRouter();
   useEffect(() => {
     router.replace('/products?new=1');
-  }, []);
+  }, [router]);
   return null;
 }
diff --git a/app/(dashboard)/sales/page.tsx b/app/(dashboard)/sales/page.tsx
index a74cdeb..2dbe4a2 100644
--- a/app/(dashboard)/sales/page.tsx
+++ b/app/(dashboard)/sales/page.tsx
@@ -89,6 +89,10 @@ export default function SalesPage() {
     getDocs(collection(db, `tenants/${tenantId}/sales/${selected.id}/sale_items`))
       .then(snap => setSaleItems(snap.docs.map(d => ({ id: d.id, ...d.data() })) as unknown as SaleItem[]))
       .finally(() => setLoadingItems(false));
+    // Seul selected?.id détermine la requête ; ajouter l'objet `selected` entier
+    // provoquerait un rechargement inutile à chaque nouvelle référence (ex.
+    // re-fetch de la liste des ventes) même pour la même vente sélectionnée.
+    // eslint-disable-next-line react-hooks/exhaustive-deps
   }, [tenantId, selected?.id]);
 
   const filtered = sales.filter(s => {
diff --git a/app/(dashboard)/stores/page.tsx b/app/(dashboard)/stores/page.tsx
index fc2707e..17dfbce 100644
--- a/app/(dashboard)/stores/page.tsx
+++ b/app/(dashboard)/stores/page.tsx
@@ -68,7 +68,7 @@ export default function StoresPage() {
       // Synchroniser avec le store global (pour le sélecteur de magasin dans le header)
       setStores(data.filter(s => s.isActive));
     });
-  }, [tenantId]);
+  }, [tenantId, setStores]);
 
   const f = (field: keyof StoreForm, value: string | boolean) => setForm(p => ({ ...p, [field]: value }));
 
diff --git a/app/(dashboard)/users/page.tsx b/app/(dashboard)/users/page.tsx
index 8e70880..13da29c 100644
--- a/app/(dashboard)/users/page.tsx
+++ b/app/(dashboard)/users/page.tsx
@@ -28,6 +28,7 @@ import { useToast } from '@/hooks/use-toast';
 import { collection, query, orderBy, onSnapshot } from 'firebase/firestore';
 import { db } from '@/lib/firebase/client';
 import { tenantCol } from '@/lib/firebase/collections';
+import { useRequireRole } from '@/components/auth/require-role';
 
 interface UserProfile {
   id: string; uid: string; email: string;
@@ -76,6 +77,7 @@ const EMPTY_EDIT_FORM: EditForm = {
 
 export default function UsersPage() {
   const router = useRouter();
+  const roleGuardAllowed = useRequireRole(['OWNER', 'ADMIN']);
   const { tenant, user: currentUser } = useAuthStore();
   const tenantId = tenant?.id;
   const { toast } = useToast();
@@ -367,6 +369,8 @@ export default function UsersPage() {
     return <span className={`inline-flex items-center gap-1 text-xs px-2 py-1 rounded-full font-medium ${cfg.color}`}><Icon className="h-3 w-3" />{cfg.label}</span>;
   };
 
+  if (!roleGuardAllowed) return null;
+
   return (
     <DashboardLayout>
       <div className="space-y-6">
diff --git a/app/api/pos/checkout/route.ts b/app/api/pos/checkout/route.ts
index eec32d0..c8f8c0c 100644
--- a/app/api/pos/checkout/route.ts
+++ b/app/api/pos/checkout/route.ts
@@ -107,31 +107,11 @@ export async function POST(request: NextRequest) {
     const acompte = paymentMethod === 'CREDIT' ? Math.max(0, Math.min(Number(amountReceived) || 0, total)) : total;
     const soldeCredit = paymentMethod === 'CREDIT' ? Math.max(0, total - acompte) : 0;
 
-    // Fix : le plafond de crédit du client (customer.creditLimit) n'était jamais
-    // vérifié — un client pouvait accumuler une dette illimitée.
-    //
-    // En synchronisation offline (offlineSyncId présent), la vente a DÉJÀ eu
-    // lieu physiquement — le client est reparti avec la marchandise pendant la
-    // coupure réseau. La refuser ici ne l'annule pas, ça la fait juste échouer
-    // silencieusement en boucle dans la file locale. On laisse donc passer et
-    // on signale le dépassement via une alerte pour régularisation manuelle,
-    // au lieu de bloquer une transaction déjà consommée dans le monde réel.
-    let creditConflict = false;
-    if (paymentMethod === 'CREDIT' && soldeCredit > 0 && customer) {
-      const creditLimit = Number(customer.creditLimit) || 0;
-      const creditUsed = Number(customer.creditUsed) || 0;
-      if (creditUsed + soldeCredit > creditLimit) {
-        if (!offlineSyncId) {
-          const disponible = Math.max(0, creditLimit - creditUsed);
-          return NextResponse.json(
-            { error: `Plafond de crédit dépassé pour ce client. Crédit disponible : ${disponible} FCFA.` },
-            { status: 400 }
-          );
-        }
-        creditConflict = true;
-      }
-    }
-
+    // Fix : le plafond de crédit du client (customer.creditLimit) doit être
+    // vérifié à l'intérieur de la transaction Firestore (voir plus bas), pas
+    // ici — une lecture faite avant la transaction peut être obsolète si deux
+    // ventes à crédit pour le même client sont lancées quasi simultanément
+    // (2 caisses, 2 vendeurs), ce qui permettait de dépasser le plafond.
     const receivedCash = paymentMethod === 'CASH' ? (Number(amountReceived) || total) : acompte;
     if (paymentMethod === 'CASH' && receivedCash < total) {
       return NextResponse.json({ error: 'Montant reçu insuffisant' }, { status: 400 });
@@ -164,9 +144,32 @@ export async function POST(request: NextRequest) {
     const counterRef = adminDb.doc(`tenants/${tenantId}/_counters/sales_${fiscalYear}`);
 
     // ── Transaction atomique : vérifie + décrémente le stock, crée la vente ─
-    const { reference, stockConflictProducts } = await adminDb.runTransaction(async (tx) => {
+    const { reference, stockConflictProducts, creditConflict } = await adminDb.runTransaction(async (tx) => {
       // ── Toutes les lectures d'abord (obligatoire dans une transaction) ────
       const counterSnap = await tx.get(counterRef);
+
+      // Lecture fraîche du client à l'intérieur de la transaction : garantit
+      // que deux ventes à crédit concurrentes pour le même client ne peuvent
+      // pas toutes les deux passer le contrôle sur la même valeur obsolète.
+      let creditConflict = false;
+      let freshCreditUsed = 0;
+      if (paymentMethod === 'CREDIT' && soldeCredit > 0 && customerRef) {
+        const freshCustomerSnap = await tx.get(customerRef);
+        const freshCustomer = freshCustomerSnap.data() || {};
+        const creditLimit = Number(freshCustomer.creditLimit) || 0;
+        freshCreditUsed = Number(freshCustomer.creditUsed) || 0;
+        if (freshCreditUsed + soldeCredit > creditLimit) {
+          // Même raisonnement que pour le stock en offline : si la vente a
+          // déjà eu lieu physiquement (offlineSyncId présent), on ne peut pas
+          // l'annuler après coup — on laisse passer et on signale.
+          if (!offlineSyncId) {
+            const disponible = Math.max(0, creditLimit - freshCreditUsed);
+            throw new Error(`Plafond de crédit dépassé pour ce client. Crédit disponible : ${disponible} FCFA.`);
+          }
+          creditConflict = true;
+        }
+      }
+
       const freshQtys: Record<string, number> = {};
       for (const l of lines) {
         const inv = invRefs[l.product.id];
@@ -213,7 +216,18 @@ export async function POST(request: NextRequest) {
       if (dedupRef) {
         tx.set(dedupRef, { saleId: saleRef.id, reference, createdAt: FieldValue.serverTimestamp() });
       }
-      return { reference, stockConflictProducts };
+      // Incrémentation atomique du crédit utilisé, dans la même transaction
+      // que la vente et le stock — remplace l'ancienne mise à jour faite
+      // après coup avec une valeur non incrémentale (`customerRef.update`),
+      // qui pouvait écraser un solde déjà modifié par une vente concurrente.
+      if (paymentMethod === 'CREDIT' && soldeCredit > 0 && customerRef) {
+        tx.update(customerRef, {
+          creditUsed: FieldValue.increment(soldeCredit),
+          updatedAt: FieldValue.serverTimestamp(),
+        });
+      }
+
+      return { reference, stockConflictProducts, creditConflict };
     });
 
     // Alerte manager si la synchronisation offline a révélé un dépassement
@@ -314,10 +328,8 @@ export async function POST(request: NextRequest) {
           createdAt: FieldValue.serverTimestamp(),
         });
       }
-      await customerRef.update({
-        creditUsed: (customer?.creditUsed || 0) + soldeCredit,
-        updatedAt: FieldValue.serverTimestamp(),
-      });
+      // Note : la mise à jour de customer.creditUsed se fait maintenant à
+      // l'intérieur de la transaction ci-dessus (FieldValue.increment), pas ici.
     }
 
     return NextResponse.json({ success: true, saleId: saleRef.id, reference, total, change });
@@ -326,6 +338,8 @@ export async function POST(request: NextRequest) {
     const msg = error instanceof Error ? error.message : 'Erreur interne';
     // Les erreurs de stock insuffisant sont utiles à afficher telles quelles
     const isStockError = msg.startsWith('Stock insuffisant');
-    return NextResponse.json({ error: isStockError ? msg : 'Erreur lors de la finalisation de la vente' }, { status: isStockError ? 409 : 500 });
+    const isCreditError = msg.startsWith('Plafond de crédit dépassé');
+    const status = isStockError ? 409 : isCreditError ? 400 : 500;
+    return NextResponse.json({ error: (isStockError || isCreditError) ? msg : 'Erreur lors de la finalisation de la vente' }, { status });
   }
 }
diff --git a/app/api/sales/cancel/route.ts b/app/api/sales/cancel/route.ts
index 212637e..fcf3e1d 100644
--- a/app/api/sales/cancel/route.ts
+++ b/app/api/sales/cancel/route.ts
@@ -4,6 +4,12 @@ import { FieldValue } from 'firebase-admin/firestore';
 import { writeAuditLog } from '@/lib/firebase/audit-log';
 import { cookies } from 'next/headers';
 
+// Seuil par défaut au-delà duquel un simple Manager ne peut plus annuler seul
+// une vente — utilisé tant que le tenant n'a pas défini son propre seuil via
+// tenants/{tenantId}.saleCancelThreshold (champ pas encore exposé dans
+// /settings ; à ajouter côté UI). Au-delà, ADMIN ou OWNER requis.
+const DEFAULT_SALE_CANCEL_THRESHOLD = 100_000; // FCFA
+
 // Annule une vente complétée et restaure le stock.
 // Migré depuis une écriture client (Firestore SDK + firestore.rules) vers
 // cette route Admin SDK pour rester cohérent avec checkout/receive/returns :
@@ -43,6 +49,19 @@ export async function POST(request: NextRequest) {
       return NextResponse.json({ error: `Impossible d'annuler une vente ${sale.status}` }, { status: 409 });
     }
 
+    // Un Manager (pas ADMIN/OWNER) ne peut annuler seul qu'en dessous du seuil
+    // configuré par le tenant (ou du seuil par défaut si pas encore configuré).
+    if (callerRole === 'MANAGER') {
+      const tenantSnap = await adminDb.doc(`tenants/${tenantId}`).get();
+      const threshold = Number(tenantSnap.data()?.saleCancelThreshold ?? DEFAULT_SALE_CANCEL_THRESHOLD);
+      if (Number(sale.total) > threshold) {
+        return NextResponse.json(
+          { error: `Cette vente dépasse le seuil d'annulation autorisé pour un Manager (${threshold.toLocaleString('fr-FR')} FCFA). Un Administrateur ou le Propriétaire doit l'annuler.` },
+          { status: 403 }
+        );
+      }
+    }
+
     const itemsSnap = await adminDb.collection(`tenants/${tenantId}/sales/${saleId}/sale_items`).get();
     const items = itemsSnap.docs.map(d => d.data() as { productId: string; quantity: number; productName: string });
 
diff --git a/components/auth/require-role.tsx b/components/auth/require-role.tsx
new file mode 100644
index 0000000..cf2362f
--- /dev/null
+++ b/components/auth/require-role.tsx
@@ -0,0 +1,62 @@
+'use client';
+
+import { useEffect, useRef } from 'react';
+import { useRouter } from 'next/navigation';
+import { useAuthStore } from '@/hooks/store';
+import { useToast } from '@/hooks/use-toast';
+import type { UserRole } from '@/lib/types';
+
+/**
+ * Garde-fou de rôle au niveau d'une page du dashboard.
+ *
+ * Fix : avant ce composant, rien (ni middleware.ts, ni un layout, ni les
+ * pages elles-mêmes) ne redirigeait un utilisateur non autorisé qui accédait
+ * directement à une URL sensible (ex. un CASHIER tapant /users). Seul
+ * sidebar-nav.tsx cachait le lien, ce qui n'est qu'un confort visuel, pas
+ * une protection.
+ *
+ * Note importante : les données restaient déjà protégées côté serveur (API
+ * Admin SDK + firestore.rules) — donc ce n'était pas une fuite de données,
+ * juste une mauvaise UX (page vide ou erreurs de lecture au lieu d'un vrai
+ * message d'accès refusé). Ce composant corrige l'UX ; il ne remplace pas
+ * la sécurité côté serveur, qui reste la vraie ligne de défense.
+ *
+ * Usage, en tête de la page concernée :
+ *   export default function UsersPage() {
+ *     const allowed = useRequireRole(['OWNER', 'ADMIN']);
+ *     if (!allowed) return null;
+ *     ...
+ *   }
+ */
+export function useRequireRole(roles: UserRole[]): boolean {
+  const router = useRouter();
+  const { toast } = useToast();
+  const user = useAuthStore((s) => s.user);
+  const isLoading = useAuthStore((s) => s.isLoading);
+  const hasWarned = useRef(false);
+
+  const allowed = !!user && roles.includes(user.role);
+
+  useEffect(() => {
+    if (isLoading) return; // attendre la fin du chargement de session avant de juger
+    if (!user) return; // pas connecté : laissé au middleware/redirection de login existants
+    if (!allowed && !hasWarned.current) {
+      hasWarned.current = true;
+      toast({
+        title: 'Accès non autorisé',
+        description: "Vous n'avez pas les droits nécessaires pour accéder à cette page.",
+        variant: 'destructive',
+      });
+      router.replace('/dashboard');
+    }
+  }, [isLoading, user, allowed, router, toast]);
+
+  return isLoading ? false : allowed;
+}
+
+/** Variante composant, pour envelopper directement le JSX d'une page si préféré au hook. */
+export function RequireRole({ roles, children }: { roles: UserRole[]; children: React.ReactNode }) {
+  const allowed = useRequireRole(roles);
+  if (!allowed) return null;
+  return <>{children}</>;
+}
diff --git a/components/layout/sidebar-nav.tsx b/components/layout/sidebar-nav.tsx
index 1e681d7..7f570c5 100644
--- a/components/layout/sidebar-nav.tsx
+++ b/components/layout/sidebar-nav.tsx
@@ -83,7 +83,7 @@ function NavItemComponent({
 
   useEffect(() => {
     if (isActive && hasChildren) setOpen(true);
-  }, [pathname]);
+  }, [pathname, isActive, hasChildren]);
 
   if (hasChildren) {
     return (
diff --git a/lib/types/index.ts b/lib/types/index.ts
index 82c15d8..fc0ad68 100644
--- a/lib/types/index.ts
+++ b/lib/types/index.ts
@@ -36,6 +36,10 @@ export interface Tenant {
   createdAt: Date;
   updatedAt: Date;
   subscription?: Subscription;
+  // Montant (FCFA) au-delà duquel un Manager seul ne peut plus annuler une
+  // vente sans validation Admin/Owner. Optionnel : si absent, une valeur par
+  // défaut est appliquée côté serveur (voir DEFAULT_SALE_CANCEL_THRESHOLD).
+  saleCancelThreshold?: number;
 }
 
 export interface Subscription {
diff --git a/package.json b/package.json
index 9efa1ce..4104445 100644
--- a/package.json
+++ b/package.json
@@ -72,6 +72,7 @@
     "tailwindcss-animate": "^1.0.7",
     "typescript": "5.2.2",
     "vaul": "^0.9.9",
+    "xlsx": "^0.18.5",
     "zod": "^3.23.8",
     "zustand": "^5.0.14"
   }
